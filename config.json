{
  "registry": "ghcr.io",
  "author": "idekube-project",
  "name": "idekube-container",
  "archs": ["amd64", "arm64"],
  "dockerargs_file": ".dockerargs.base",
  "lineups": {
    "base": {
      "dockerargs_file": ".dockerargs.base"
    },
    "ascend": {
      "dockerargs_file": ".dockerargs.ascend",
      "archs": ["arm64"]
    }
  }
}
